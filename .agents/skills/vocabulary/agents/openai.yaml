interface:
  display_name: "HaiLibrary Vocabulary"
  short_description: "创建、核实并生成多语言词条与无字词卡"
  default_prompt: "Use $vocabulary to create or revise this vocabulary entry, verify it with live evidence, generate its wordless card, and self-check the result."

